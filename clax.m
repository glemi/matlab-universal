function clax()
    hold off;
    plot(0);
    cla;
    hold all;
	
% 	hParent = get(gca, 'Parent');
% 	h = findobj('Parent', hParent, 'Type', 'axes');
% 	h = setdiff(h, gca);
% 	
% 	if ~isempty(h)
% 		delete(h);
% 	end
end

