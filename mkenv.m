function mkenv()
    
    envName = '';
    envDescirption = '';
    
    mkdir(envName);
    
    initfile = fullfile(envName, 'init.m');
    infofile = fullfile(envName, 'info.m');
    
    fId = fopen(initfile, 'w');
    fprintf()    
    

end

